__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# Intermediate TF export artifacts (not needed for Torch runtime).
exported_tf/

# Upstream TensorFlow SavedModels (keep locally; not shipped in torch-maps).
timbuck_eng.tf/
ensemble_model/
